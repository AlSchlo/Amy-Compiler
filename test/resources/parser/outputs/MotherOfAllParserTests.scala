object MotherOfAllParserTests

  fn father(): Unit = {
    (if(()) {
      32
    } else {
      1
    }) match {
      case () =>
        (if(()) {
          (if(!("son")) {
            (
              ();
              true;
              ()
            )
          } else {
            (5545 / false)
          })
        } else {
          (
            val x: String =
              (false && 78);
            !(7)
          )
        })
    };
    val a: Unit =
      12;
    -(father());
    (!(()) ++ -(()))
  }

  fn child(a: Unit): Boolean = {
    (if((
      true match {
        case _ =>
          ()
      } match {
        case _ =>
          x
      };
      !(())
    )) {
      "something"
    } else {
      54554
    })
  }

  -(Aasdasd.father((), child(a)));
  ("I'm happy I was able to finish" ++ ("to parse this one" / -(12)));
  ()
end MotherOfAllParserTests
